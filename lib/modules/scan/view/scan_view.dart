import 'package:flutter/widgets.dart';

class ScanView extends StatelessWidget {
  const ScanView({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('ScanView'));
  }
}
